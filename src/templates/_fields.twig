{% macro general(company) %}
{{ forms.textField({
    label: "Info"|t('companies-management'),
    site: company.site,
    id: 'info',
    name: 'info',
    value: company.info,
    errors: company.getErrors('info')|merge(company.getErrors('uri'))
}) }}
{% endmacro %}